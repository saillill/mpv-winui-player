<local:OptionControlBase
    x:Class="mpv_winui.Modules.Settings.Controls.ControlBarCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mpv_winui.Modules.Settings.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel x:Name="RootPanel" Spacing="8">
        <TextBlock
            x:Name="BarHint"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap" />

        <!-- One horizontal row, like the real progress bar: fixed transport
             cells + shown movable cells in the real layout order, with small
             gaps where the real bar separates its groups. -->
        <ScrollViewer
            HorizontalScrollBarVisibility="Auto"
            HorizontalScrollMode="Enabled"
            VerticalScrollBarVisibility="Disabled"
            VerticalScrollMode="Disabled">
            <StackPanel
                x:Name="BarPanel"
                Orientation="Horizontal"
                Spacing="3"
                VerticalAlignment="Center" />
        </ScrollViewer>

        <!-- Candidate drawer: hidden buttons, drag one back onto the bar to show it. -->
        <TextBlock
            x:Name="AvailableCaption"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <ItemsControl
            x:Name="AvailableBar"
            IsTabStop="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Horizontal" ItemWidth="48" ItemHeight="48" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</local:OptionControlBase>
