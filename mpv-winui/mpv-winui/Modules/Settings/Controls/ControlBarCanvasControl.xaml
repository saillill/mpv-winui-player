<local:OptionControlBase
    x:Class="mpv_winui.Modules.Settings.Controls.ControlBarCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mpv_winui.Modules.Settings.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel x:Name="RootPanel" Spacing="10">
        <!-- Fixed transport group (locked, always visible) -->
        <TextBlock
            x:Name="FixedCaption"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <StackPanel
            x:Name="FixedBar"
            Orientation="Horizontal"
            Spacing="8" />

        <!-- Shown (reorderable) buttons -->
        <TextBlock
            x:Name="ShownCaption"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <StackPanel
            x:Name="ShownBar"
            Orientation="Horizontal"
            Spacing="8" />

        <!-- Hidden (available) buttons — the "drawer", like Android launchers -->
        <TextBlock
            x:Name="AvailableCaption"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <ItemsControl x:Name="AvailableBar" IsTabStop="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Horizontal" ItemWidth="88" ItemHeight="88" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</local:OptionControlBase>
