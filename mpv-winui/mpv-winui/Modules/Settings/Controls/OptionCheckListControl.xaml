<local:OptionControlBase
    x:Class="mpv_winui.Modules.Settings.Controls.OptionCheckListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:mpv_winui.Modules.Settings.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="CheckWrapPanel">
            <ItemsWrapGrid
                ItemHeight="36"
                ItemWidth="170"
                Orientation="Horizontal" />
        </ItemsPanelTemplate>
        <ItemsPanelTemplate x:Key="CheckListPanel">
            <StackPanel Spacing="4" />
        </ItemsPanelTemplate>
    </UserControl.Resources>

    <Grid
        VerticalAlignment="Center"
        Background="Transparent"
        Tapped="OnCardTapped">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock
                    x:Name="LabelText"
                    Style="{StaticResource BodyTextBlockStyle}"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="DescriptionText"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    TextWrapping="Wrap"
                    Visibility="Collapsed" />
                <TextBlock
                    x:Name="WarningText"
                    Foreground="{ThemeResource SystemFillColorCautionBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    TextWrapping="Wrap"
                    Visibility="Collapsed" />
            </StackPanel>
            <FontIcon
                x:Name="ExpandIcon"
                Grid.Column="1"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                FontSize="12"
                Glyph="&#xE70D;" />
        </Grid>
        <Grid
            x:Name="ExpandedPanel"
            Grid.Row="1"
            Margin="0,12,0,0"
            Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel
                x:Name="CheckItemsControl"
                Grid.Row="0"
                Spacing="4" />
            <Button
                x:Name="ApplyButton"
                Grid.Row="1"
                MinWidth="88"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Click="OnApplyClick"
                Style="{StaticResource AccentButtonStyle}"
                Visibility="Collapsed" />
        </Grid>
    </Grid>
</local:OptionControlBase>
