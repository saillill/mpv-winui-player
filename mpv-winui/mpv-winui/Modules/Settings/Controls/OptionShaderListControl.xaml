<local:OptionControlBase
    x:Class="mpv_winui.Modules.Settings.Controls.OptionShaderListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mpv_winui.Modules.Settings.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="8">
        <StackPanel Spacing="2">
            <TextBlock
                x:Name="LabelText"
                Style="{StaticResource BodyTextBlockStyle}"
                TextWrapping="Wrap" />
            <TextBlock
                x:Name="DescriptionText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
            <TextBlock
                x:Name="WarningText"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
        </StackPanel>

        <ItemsControl x:Name="ShaderItems">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:ShaderEntry">
                    <Grid
                        Margin="0,2"
                        ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Checked="Entry_Changed"
                            IsChecked="{x:Bind Enabled, Mode=TwoWay}"
                            Unchecked="Entry_Changed" />
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{x:Bind Path}"
                            TextTrimming="CharacterEllipsis"
                            ToolTipService.ToolTip="{x:Bind Path}" />
                        <Button
                            Grid.Column="2"
                            Loaded="MoveUpButton_Loaded"
                            Content="&#xE70E;"
                            Tag="{x:Bind}"
                            Click="MoveUp_Click" />
                        <Button
                            Grid.Column="3"
                            Loaded="MoveDownButton_Loaded"
                            Content="&#xE70D;"
                            Tag="{x:Bind}"
                            Click="MoveDown_Click" />
                        <Button
                            Grid.Column="4"
                            Loaded="RemoveButton_Loaded"
                            Content="&#xE711;"
                            Tag="{x:Bind}"
                            Click="Remove_Click" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button
            x:Name="AddButton"
            HorizontalAlignment="Left"
            Click="Add_Click" />
    </StackPanel>
</local:OptionControlBase>
