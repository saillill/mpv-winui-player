<Window
    x:Class="mpv_winui.Modules.Settings.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <!-- Slim search-pill TextBox: remove the stock 32px MinHeight so the
                 28px AutoSuggestBox keeps its full rounded silhouette, and add
                 left padding so the overlay magnifier never collides with the
                 placeholder/text. The TextBox keeps the stock template, so the
                 focused accent border (the "blue bar") still comes from the
                 built-in Focused visual state. -->
            <Style x:Key="SearchTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Padding" Value="32,5,10,5" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FontSize" Value="14" />
            </Style>

            <Style x:Key="BareAutoSuggestBoxStyle" TargetType="AutoSuggestBox">
                <Setter Property="TextBoxStyle" Value="{StaticResource SearchTextBoxStyle}" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="AutoSuggestBox">
                            <Grid x:Name="LayoutRoot">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBox x:Name="TextBox"
                                         Style="{TemplateBinding TextBoxStyle}"
                                         PlaceholderText="{TemplateBinding PlaceholderText}"
                                         Width="{TemplateBinding Width}"
                                         Height="{TemplateBinding Height}"
                                         Foreground="{ThemeResource TextControlForeground}"
                                         Background="{ThemeResource TextControlBackground}"
                                         BorderBrush="{ThemeResource TextControlBorderBrush}"
                                         BorderThickness="{ThemeResource TextControlBorderThemeThickness}"
                                         FontSize="{TemplateBinding FontSize}"
                                         FontFamily="{TemplateBinding FontFamily}"
                                         FontWeight="{TemplateBinding FontWeight}"
                                         FontStretch="{TemplateBinding FontStretch}"
                                         VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                         ScrollViewer.BringIntoViewOnFocusChange="False"
                                         Canvas.ZIndex="0"
                                         Margin="0"
                                         DesiredCandidateWindowAlignment="BottomEdge"
                                         UseSystemFocusVisuals="{TemplateBinding UseSystemFocusVisuals}"
                                         CornerRadius="{TemplateBinding CornerRadius}" />
                                <Popup x:Name="SuggestionsPopup" ShouldConstrainToRootBounds="False">
                                    <Border x:Name="SuggestionsContainer"
                                            Padding="{ThemeResource AutoSuggestListMargin}"
                                            BorderThickness="{ThemeResource AutoSuggestListBorderThemeThickness}"
                                            BorderBrush="{ThemeResource AutoSuggestBoxSuggestionsListBorderBrush}"
                                            Background="{ThemeResource AutoSuggestBoxSuggestionsListBackground}"
                                            CornerRadius="{ThemeResource OverlayCornerRadius}">
                                        <ListView x:Name="SuggestionsList"
                                                  DisplayMemberPath="{TemplateBinding DisplayMemberPath}"
                                                  IsItemClickEnabled="True"
                                                  ItemTemplate="{TemplateBinding ItemTemplate}"
                                                  ItemTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  ItemContainerStyle="{TemplateBinding ItemContainerStyle}"
                                                  MaxHeight="{ThemeResource AutoSuggestListMaxHeight}"
                                                  Margin="{ThemeResource AutoSuggestListPadding}" />
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Windows-settings top bar: title left, pill search center. The
             magnifier is overlaid inside the pill (drawn on top of the TextBox),
             the TextBox has left padding so caption text never overlaps it, and
             the box height (28) rides the template binding. -->
        <Border
            Grid.Row="0"
            Padding="16,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="SettingsTitleText"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold" />
                <Grid
                    Grid.Column="1"
                    Width="340"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <!-- Magnifier is inside the pill: placed after the search
                         box so it draws on top of the TextBox surface, with the
                         TextBox's left padding keeping the placeholder clear.
                         Height is kept at 28 and the TextBox MinHeight is zeroed
                         by SearchTextBoxStyle, so the bottom corners are not
                         clipped by the stock 32px minimum. -->
                    <AutoSuggestBox
                        x:Name="SearchBox"
                        Height="28"
                        CornerRadius="14"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        Style="{StaticResource BareAutoSuggestBoxStyle}"
                        PlaceholderText="Find a setting"
                        QuerySubmitted="SearchBox_QuerySubmitted"
                        SuggestionChosen="SearchBox_SuggestionChosen"
                        TextChanged="SearchBox_TextChanged" />
                    <FontIcon
                        Glyph="&#xE721;"
                        FontSize="14"
                        Margin="12,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        IsHitTestVisible="False" />
                </Grid>
            </Grid>
        </Border>
        <Frame
            x:Name="PageFrame"
            Grid.Row="1"
            Loaded="PageFrame_Loaded" />
    </Grid>
</Window>