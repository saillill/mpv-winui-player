<Window
    x:Class="mpv_winui.Modules.Settings.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <!-- Stock AutoSuggestBox template, plus Padding/Height passthrough
                 into its inner TextBox. The SDK template binds only Width and
                 CornerRadius there, so caption spacing and row height set on
                 the control were silently ignored: the caption started at
                 the default ~11px inset and collided with the leading glyph,
                 and the box could not be slimmed. Only stock theme keys and
                 parts are referenced here, so nothing extra can fail at
                 runtime. -->
            <Style x:Key="BareAutoSuggestBoxStyle" TargetType="AutoSuggestBox">
                <Setter Property="TextBoxStyle" Value="{StaticResource DefaultTextBoxStyle}" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="AutoSuggestBox">
                            <Grid x:Name="LayoutRoot">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBox x:Name="TextBox"
                                         Style="{TemplateBinding TextBoxStyle}"
                                         PlaceholderText="{TemplateBinding PlaceholderText}"
                                         Width="{TemplateBinding Width}"
                                         Height="{TemplateBinding Height}"
                                         Padding="{TemplateBinding Padding}"
                                         Foreground="{ThemeResource TextControlForeground}"
                                         Background="{ThemeResource TextControlBackground}"
                                         BorderBrush="{ThemeResource TextControlBorderBrush}"
                                         BorderThickness="{ThemeResource TextControlBorderThemeThickness}"
                                         FontSize="{TemplateBinding FontSize}"
                                         FontFamily="{TemplateBinding FontFamily}"
                                         FontWeight="{TemplateBinding FontWeight}"
                                         FontStretch="{TemplateBinding FontStretch}"
                                         VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                         ScrollViewer.BringIntoViewOnFocusChange="False"
                                         Canvas.ZIndex="0"
                                         Margin="0"
                                         DesiredCandidateWindowAlignment="BottomEdge"
                                         UseSystemFocusVisuals="{TemplateBinding UseSystemFocusVisuals}"
                                         CornerRadius="{TemplateBinding CornerRadius}" />
                                <Popup x:Name="SuggestionsPopup" ShouldConstrainToRootBounds="False">
                                    <Border x:Name="SuggestionsContainer"
                                            Padding="{ThemeResource AutoSuggestListMargin}"
                                            BorderThickness="{ThemeResource AutoSuggestListBorderThemeThickness}"
                                            BorderBrush="{ThemeResource AutoSuggestBoxSuggestionsListBorderBrush}"
                                            Background="{ThemeResource AutoSuggestBoxSuggestionsListBackground}"
                                            CornerRadius="{ThemeResource OverlayCornerRadius}">
                                        <ListView x:Name="SuggestionsList"
                                                  DisplayMemberPath="{TemplateBinding DisplayMemberPath}"
                                                  IsItemClickEnabled="True"
                                                  ItemTemplate="{TemplateBinding ItemTemplate}"
                                                  ItemTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  ItemContainerStyle="{TemplateBinding ItemContainerStyle}"
                                                  MaxHeight="{ThemeResource AutoSuggestListMaxHeight}"
                                                  Margin="{ThemeResource AutoSuggestListPadding}" />
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Windows-settings top bar: title left, pill search center. The
             glyph is a separate non-interactive element with its own left
             margin; the caption starts at Padding.Left (34) so they never
             overlap, and the box height (28) rides the template binding. -->
        <Border
            Grid.Row="0"
            Padding="16,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="SettingsTitleText"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold" />
                <Grid
                    Grid.Column="1"
                    Width="340"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <AutoSuggestBox
                        x:Name="SearchBox"
                        Height="28"
                        Padding="34,0,10,0"
                        CornerRadius="14"
                        Style="{StaticResource BareAutoSuggestBoxStyle}"
                        PlaceholderText="Find a setting"
                        QuerySubmitted="SearchBox_QuerySubmitted"
                        SuggestionChosen="SearchBox_SuggestionChosen"
                        TextChanged="SearchBox_TextChanged" />
                    <FontIcon
                        Glyph="&#xE721;"
                        FontSize="12"
                        Margin="14,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        IsHitTestVisible="False" />
                </Grid>
            </Grid>
        </Border>
        <Frame
            x:Name="PageFrame"
            Grid.Row="1"
            Loaded="PageFrame_Loaded" />
    </Grid>
</Window>